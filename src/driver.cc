#include <iostream>

#include "dna_strand.hpp"

int main() {
    /*
    //DNAstrand test = DNAstrand(*(new Node('c')));
    DNAstrand test = DNAstrand(*(new Node('g')));
    //test.PushBack('t');
    //test.PushBack('g');
    test.PushBack('a');
    test.PushBack('a');
    test.PushBack('t');
    test.PushBack('t');
    test.PushBack('c');
    test.PushBack('a');
    //test.PushBack('g');
    DNAstrand to_splice = DNAstrand(*(new Node('t')));
    to_splice.PushBack('g');
    to_splice.PushBack('a');
    to_splice.PushBack('t');
    to_splice.PushBack('c');
    test.SpliceIn("gaattc", to_splice);
    std::cout << test.ToString() << std::endl;
    */

}
